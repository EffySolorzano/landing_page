import React from 'react';

const Header =() => {
    return(
        <header>
            <nav>
                <h1> title</h1>
            </nav>
        </header>
    );
};

export default Header; 